doctype html
html(lang='en-us')
  block html

  head
    meta(charset='utf-8')

    meta(name='keywords', content='portfolio, programming, blog')
    meta(name='viewport', content='width=device-width, initial-scale=1')

    meta(name='description', content='Portfolio site, programming blog')
    meta(name='author', content='Edvinas')

    link(rel='shortcut icon', type='image/x-icon' href='/_images/cool_dog.ico')
    link(rel='stylesheet' href='/_styles/main.css')

    if title
      title #{title}
    else
      title Edds
  body
    main
      article
        if title || date
          header
            if title
              h1 #{title }
            if date
              time(datetime=`${date}`) #{date}
        block content
        footer
          | Contact me via
          | #[a(target='_blank' href='https://github.com/Edvinas01') GitHub]
